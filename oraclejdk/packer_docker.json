{
  "variables": {
    "ansible_host": "default",
    "ansible_connection": "docker",
    "package": "oraclejdk",
    "package_version": "{{env `JAVA_VERSION`}}",
    "username": "{{env `DOCKER_USERNAME`}}",
    "version": "{{isotime \"20060102.030405\"}}",
    "version_simple": "{{isotime \"20060102\"}}"
  },
  "builders": [
    {
      "type": "docker",
      "image": "ubuntu:16.04",
      "commit": "true",
      "run_command": [
        "-d",
        "-i",
        "-t",
        "--name",
        "{{user `ansible_host`}}",
        "{{.Image}}",
        "/bin/bash"
      ],
      "changes": [
        "ENV JAVA_HOME /usr/lib/jvm/java-11-oracle",
        "ENTRYPOINT [\"/usr/local/bin/docker-entrypoint\"]",
        "WORKDIR /usr/bin",
        "CMD [\"bash\"]"
      ]
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "apt-get update",
        "apt-get upgrade -y",
        "apt-get install -y aptitude python-minimal git apt-transport-https ca-certificates-java bzip2 unzip xz-utils -yq"
      ]
    },
    {
      "type": "shell-local",
      "command": "ansible-galaxy install --force -v --roles-path='./ansible/roles' --role-file='./ansible/requirements.yml'"
    },
    {
      "type": "ansible",
      "user": "root",
      "playbook_file": "./ansible/playbook_local.yml",
      "extra_arguments": [
        "--extra-vars",
        "ansible_host={{user `ansible_host`}} ansible_connection={{user `ansible_connection`}}"
      ]
    },
    {
      "type": "file",
      "source": "./files/docker-entrypoint",
      "destination": "/usr/local/bin/docker-entrypoint"
    },
    {
      "type": "shell",
      "inline": [
        "java -version"
      ]
    }
  ],
  "post-processors": [
    [
      {
        "type": "docker-tag",
        "repository": "{{ user `username` }}/{{user `package`}}",
        "tag": "{{user `package_version`}}"
      },
      {
        "type": "docker-tag",
        "repository": "{{ user `username` }}/{{user `package`}}",
        "tag": "{{user `version_simple`}}"
      },
      {
        "type": "docker-tag",
        "repository": "{{ user `username` }}/{{user `package`}}"
      }
    ]
  ]
}
